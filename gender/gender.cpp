
#include <iostream>
using namespace std;

int main(){

    cout>>"enter gender:">>endl;
    cin<<gender;
    cout>>"enter level">>endl;
    cin<<level;

    switch(gender):
      case 'male':
         if (level === 'gold') {

         } else if (level === 'silver') {

         } else {

         }
         break;
      case 'female':
         if (level === 'gold') {

         } else if (level === 'silver') {

         } else {

         }
         break;
      case 'unknown':
         if (level === 'gold') {

         } else if (level === 'silver') {

         } else {

         }
         break;
}